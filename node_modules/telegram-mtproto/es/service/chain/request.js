import axios from '../../http';

import Bluebird from 'bluebird';

var basicConfig = {
  responseType: 'arraybuffer'
};

var request = config => {
  var fullConfig = Object.assign({}, basicConfig, config);
  var result = Bluebird.try(axios.post, ['a', fullConfig]);
  return result;
};

export default request;
//# sourceMappingURL=request.js.map