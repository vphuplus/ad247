//@flow

import type { Arr } from 'flow-static-land/src/Arr'
import type { Tuple } from 'flow-static-land/src/Tuple'

export type SupportedProtocol =
  'http'
  | 'https'

export type DcConfig = {|
  id: number,
  host: string,
|}

export type Config = {|
  path: string,
  protocol: SupportedProtocol,
  port: number,
  dcList: DcConfig[],
|}

export type ConfigError = Arr<string>

export type ErrorChecker = {
  test(obj: ServerConfigFull): boolean,
  error(obj: ServerConfigFull): string
}

export type ConfigCheck = Arr<ErrorChecker>
